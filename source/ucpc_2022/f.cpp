#include <iostream>
#include <vector>
#include <algorithm>
#include <string>
#include <cmath>
#include <queue>
#include <functional>
typedef long long ll;
const ll MOD = 1e9 + 7;
const ll INF = 0x3fffffff;
using namespace std;

int main(void)
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	int n, m;
	cin >> n >> m;
	vector<string> board(n);
	for (int i = 0; i < n; i++)
	{
		cin >> board[i];
	}

	

}