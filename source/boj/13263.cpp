#include <iostream>
#include <vector>
#include <cmath>
#include <algorithm>
typedef long long ll;
typedef long double ld;
using namespace std;

int main(void)
{
	int n;
	cin >> n;
	vector<ll> a(n), b(n);
	for (auto& elem : a) cin >> elem;
	for (auto& elem : b) cin >> elem;


}